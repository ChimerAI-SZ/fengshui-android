name: Build debug APK

on:
  push:
    branches: [ main ]
    tags: [ "v*" ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate map key secrets
        shell: bash
        run: |
          if [ -z "${{ secrets.GOOGLE_MAPS_API_KEY }}" ] || [ -z "${{ secrets.AMAP_API_KEY }}" ]; then
            echo "Missing required secrets: GOOGLE_MAPS_API_KEY / AMAP_API_KEY"
            exit 1
          fi

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle (no cache)
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.6"
          cache-disabled: true

      - name: Create local.properties for CI
        shell: bash
        run: |
          echo "sdk.dir=$ANDROID_HOME" > local.properties
          echo "GOOGLE_MAPS_API_KEY=${{ secrets.GOOGLE_MAPS_API_KEY }}" >> local.properties
          echo "AMAP_API_KEY=${{ secrets.AMAP_API_KEY }}" >> local.properties

      - name: Build debug APK
        run: gradle :app:assembleDebug --no-daemon --stacktrace

      - name: Upload debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: fengshui-debug
          path: app/build/outputs/apk/debug/app-debug.apk

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download debug APK artifact
        uses: actions/download-artifact@v4
        with:
          name: fengshui-debug
          path: dist

      - name: Publish GitHub Release with APK
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            dist/app-debug.apk
